<Window x:Class="Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Views"
        mc:Ignorable="d"
        Title="Al Burak Marine" Height="200" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Background="DarkBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Source="/Resources/logo.png" Width="150"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Password" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <PasswordBox x:Name="passwordTb" Grid.Column="1" FontSize="16" Width="150" Height="30" HorizontalAlignment="Left"/>
        </Grid>

        <Button Grid.Row="2" Click="Button_Click" Content="Login" Background="White" Width="100" Height="30" FontSize="13" Foreground="DarkBlue"/>

    </Grid>
</Window>
